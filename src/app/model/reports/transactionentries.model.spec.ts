import { Transactionentries } from './transactionentries.model';

describe('Transactionentries', () => {
  it('should create an instance', () => {
    expect(new Transactionentries()).toBeTruthy();
  });
});
